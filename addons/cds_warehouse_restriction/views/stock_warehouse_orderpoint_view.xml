<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="stock.menu_reordering_rules_replenish" model="ir.ui.menu">
        <field name="group_ids"
               eval="[(4, ref('stock.group_stock_user'))]"/>


    </record>

    <record id="stock.action_orderpoint_replenish" model="ir.actions.act_window">

        <field name="domain">[('id','=',False)]</field>

    </record>

    <!-- <record id="action_orderpoint_allowed_replenish" model="ir.actions.act_window">
         <field name="name">Replenishment</field>
         <field name="res_model">stock.warehouse.orderpoint</field>
         <field name="type">ir.actions.act_window</field>
         <field name="view_mode">tree</field>
         <field name="domain">[('id', '=', False)]</field>
         <field name="view_id" ref="stock.view_warehouse_orderpoint_tree_editable"/>
         <field name="search_view_id" ref="stock.stock_reorder_report_search"/>

     </record>

      <record id="action_allowed_replenish_server"
                 model="ir.actions.server">
             <field name="name">Allowed Replenishment</field>
             <field name="model_id" ref="stock.model_stock_warehouse_orderpoint"/>
             <field name="state">code</field>
             <field name="code">action = model.action_view_allowed_replenish()</field>
         </record>

     <menuitem id="menu_action_allowed_replenish"
                   name="Allowed Replenishment"
                   parent="stock.menu_stock_root"
                    groups="stock.group_stock_user"
                   action="cds_warehouse_restriction.action_picking_type_branch_action_server"
                   sequence="1"/>-->
</odoo>

